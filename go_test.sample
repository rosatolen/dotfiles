package main

import (
	"fmt"
	"testing"

	. "gopkg.in/check.v1"
)

func Test(t *testing.T) { TestingT(t) }

type TestSuite struct{}

var _ = Suite(&TestSuite{})

func (s *TestSuite) Test_passwordHasMinimumWords(c *C) {
	c.Assert(true, isTrue)
}
